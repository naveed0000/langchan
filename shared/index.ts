/**
 * Cross-cutting concerns shared by app/, ai/, and modules/ (types, error
 * classes, logging). Add files here only when at least two of those layers
 * need the same code — module-specific logic belongs in modules/<name>.
 */
export {};
